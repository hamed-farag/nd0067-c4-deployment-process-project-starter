{
  "scripts": {
    "frontend:open": "cd udagram-frontend",
    "backend:open": "cd udagram-api",
    "package:install": "npm install",
    "project:build": "npm run build",
    "project:deploy": "npm run deploy",
    "project:test": "npm run test",
    "/": "/",
    "frontend:install": "run-s frontend:open package:install",
    "backend:install": "run-s backend:open package:install",
    "frontend:build": "run-s frontend:open project:build",
    "backend:build": "run-s backend:open project:build",
    "frontend:deploy": "run-s frontend:open project:deploy",
    "backend:deploy": "run-s backend:open project:deploy",
    "frontend:test": "run-s frontend:open project:test",
    "backend:test": "run-s backend:open project:test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
